# PowerClan – Tests gehören nicht auf den Live-Server.
# Falls doch: alles blockieren.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
